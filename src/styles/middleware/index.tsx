import { createStyles, Theme, makeStyles } from '@material-ui/core/styles';

export const MiddleStyle = makeStyles((theme: Theme) =>
  createStyles({
    root: {
      position: 'relative',
      padding: '0 50px'
    }
  })
)